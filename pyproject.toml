[project]
name = "weather-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "felpmrts", email = "felipe_martins__@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
    "psycopg2-binary>=2.9.13",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "sqlalchemy>=2.0.53",
]

[project.scripts]
weather-pipeline = "weather_pipeline:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
